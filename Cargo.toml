[package]
name = "glide"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18", default-features = false, features = ["2d"] }

# Fast dev compiles
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
